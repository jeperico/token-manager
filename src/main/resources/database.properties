# Database Configuration
# Update these values according to your PostgreSQL setup

# For Docker
db.url=jdbc:postgresql://postgres:5432/token_manager
db.user=postgres
db.password=postgres

# For local development (uncomment if not using Docker)
# db.url=jdbc:postgresql://localhost:5432/token_manager
# db.user=perico
# db.password=password

# Optional: Connection pool settings
db.maxConnections=10
db.connectionTimeout=30000
